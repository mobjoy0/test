name: CI

on:
  workflow_call:

jobs:
  lint:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "lint disabled"

  test:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "tests disabled"